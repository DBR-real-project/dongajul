# ── SSH Keys ─────────────────────────────────────────────────────────────────
*.pem

# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/
*.egg-info/

# ── 환경 변수 (절대 커밋 금지) ──────────────────────────────────────────────
.env
!.env.example

# ── Node.js / React ──────────────────────────────────────────────────────────
node_modules/
.pnpm-store/
.pnpm-lock.yaml

# ── 빌드 산출물 ──────────────────────────────────────────────────────────────
frontend/dist/
frontend/.vite/
dist/
build/

# ── 대용량 데이터 파일 (git에 올리지 않음) ────────────────────────────────────
크롤링/*.csv
크롤링/*.xlsx
크롤링/*.xls
크롤링/naver/*.json
데이터처리/output/*.npy
데이터처리/output/*.parquet
데이터처리/output/*.pkl
데이터처리/output/*.index
데이터처리/output/*.txt

# ── 크롤링 CSV 예외 (DBR 제외 — 190MB 초과) ─────────────────────────────────
!크롤링/HBR_articles.csv
!크롤링/HBS_articles.csv
!크롤링/HBS_articles_ko.csv

# ── 운영 파일 예외 (팀원 공유 필요, 각 파일 100MB 이내) ─────────────────────
!데이터처리/output/umap_coords.parquet
!데이터처리/output/cluster_info.parquet
!데이터처리/output/umap_coords_v3.parquet
!데이터처리/output/cluster_info_v3.parquet
!데이터처리/output/articles_meta.parquet
!데이터처리/output/risk_model.pkl
!데이터처리/output/faiss.index
!데이터처리/output/risk_model_report.txt
!데이터처리/output/embeddings.npy
!데이터처리/output/DBR_embeddings.npy
!데이터처리/output/HBR_embeddings.npy
!데이터처리/output/HBR_labeled.parquet
!데이터처리/output/HBR_preprocessed.parquet
!데이터처리/output/HBS_embeddings.npy
!데이터처리/output/HBS_labeled.parquet
!데이터처리/output/HBS_preprocessed.parquet
!데이터처리/output/HBS_meta.parquet
!데이터처리/output/embeddings_with_hbs.npy
!데이터처리/output/faiss_with_hbs.index
!데이터처리/output/articles_meta_with_hbs.parquet
!데이터처리/output/HBR_preprocessed.parquet

# ── DB / Docker ──────────────────────────────────────────────────────────────
mysql_data/

# ── Jupyter ──────────────────────────────────────────────────────────────────
.ipynb_checkpoints/

# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ──────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp

# ── 프로젝트 로컬 전용 (팀원 공유 불필요) ────────────────────────────────────
.claude/
클로드학습자료/
/scripts/
CLAUDE.md
서류/*.pdf
